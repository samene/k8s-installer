server = "{{item.scheme|default('https')}}://{{item.endpoint}}"

[host."{{item.scheme|default('https')}}://{{item.endpoint}}"]
{%- if item.insecure_skip_verify|default(false) %}

  skip_verify = true
{% endif %}

